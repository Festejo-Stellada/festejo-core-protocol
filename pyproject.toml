[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "festejo-core"
version = "0.1.0"
dependencies = [
    "pydantic",
    "festejo-core-rust",
    "festejo-stellar"
]
